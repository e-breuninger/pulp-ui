services:
  pulp-ui:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8000"
    env_file: .env
    # Uncomment to serve deployment-specific branding from ./custom.
    # environment:
    #   PULP_UI_CUSTOM_DIR: /custom
    # volumes:
    #   - ./custom:/custom:ro
